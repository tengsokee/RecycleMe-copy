"""Landing apps.py

This script defines configurations for the landing application. 

"""
from django.apps import AppConfig


class LandingConfig(AppConfig):
    """defines configurations for the landing application. 
    """
    name = 'landing'
